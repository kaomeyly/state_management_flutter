OnReady and OnInit it a same.
- State management
    - ui
    - logic
- Getx, Provider , RiverPod, Bloc ...
- GetX : State management , Navigation, DI , ...
- SetState -> rebuild
- Reactivr widget : GetX, Obx if we can use this must to have Reactive Variable
- Reactive Variable is for updata data : obs Ex: int a = 10; RxInt , RxString, RxBool and obs is Reactive
Ex: RxInt a = 10.obs;
    var a = 20.40.obs;

- if we can use getx we have 2 screen is ui and logic.


- GetX worker : a listener that reacr when rx is change
    - ever : trov ban hav everytime del Rx variable change function
    - overAll : trov ban have pel del RxList variable change function
    - once : is only work only one
    - deboiunce or bounce : when list is work when we set min so boiunce work.
    - interval : is loop when user select
    
maincontroller 
    - home 
        - var mc = maincontroller()
    - profile 
        - var mc = maincontroller()

Dependency Injection:
  - class controller
    - class 1:
        - var cont = Get.put(controller())
    - class 2: 
        - var cont = Get.find<controller>()

       
put -> Init
lazy put -> find -> init // call is work

- Navigation : 
    - Get.to(screen2()); //use for go to page 2
    - Get.off(screen2()); //use for off screen back
    - Get.offAll(screen2()); //use for close on screen when go to another page

Route : 
    - Get.toNamed("/screen2); //use for go to page 2
    - Get.offNamed("/screen2); //use for off screen back
    - Get.offAllNamed("/screen2); //use for close on screen when go to another page

Send data : 
    - Get.toNamed("/screen2", argument : object);
    - Get.argument ["key"]
    - var arg = Get.argument'

